{
  "name": "@ts-monorepo/core",
  "version": "1.0.0",
  "description": "Core contains pure logic with no ui",
  "author": "Elias Andualem",
  "license": "MIT",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -b",
    "clean": "rm -rf ./dist && rm -f tsconfig.tsbuildinfo",
    "test": "jest",
    "test:watch": "jest --watch",
    "watch": "tsc -b -w --preserveWatchOutput"
  }
}
