{
  "name": "@ts-monorepo/bar",
  "version": "1.0.0",
  "description": "Bar contains UI",
  "author": "Elias Andualem",
  "license": "MIT",
  "private": true,
  "main": "dist/index",
  "types": "dist/index",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -b",
    "clean": "rm -rf ./dist && rm -rf ./build && rm -f tsconfig.tsbuildinfo"
  },
  "dependencies": {
    "@ts-monorepo/foo": "^1.0.0"
  }
}
